{
  "name": "@remrob/redis",
  "version": "0.0.2",
  "description": "Shared MySQL library",
  "keywords": [
    "database",
    "redis"
  ],
  "homepage": "https://bitbucket.org/remrob/r2ecosystem",
  "repository": "https://bitbucket.org/remrob/r2ecosystem",
  "license": "SEE LICENSE IN LICENSE",
  "author": "Muslim Idris <gelamu@gmail.com>",
  "main": "src/index.ts",
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "bun run clean; bun build --target bun --entrypoint ./src/index.ts",
    "clean": "rm -rf dist node_modules ../../.build-cache/redis.tsbuildinfo",
    "prepack": "bun run build",
    "sort-json-files": "sortjson *.json && prettier -w *.json"
  },
  "dependencies": {
    "@remrob/drizzle": "^0.0.0",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "json-sort-cli": "^1",
    "prettier": "^2",
    "typescript": "^5.5.4"
  }
}
